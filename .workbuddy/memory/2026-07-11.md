# 2026-07-11

## 代码审查 - 经侦大队工作记录管理系统 (jingzong-react-office-0616)
对 React 19 + TS + Vite + AntD6 + Zustand + ECharts + Electron 项目做了 7 维度全面代码审查。
- 输出报告：`CODE_REVIEW_REPORT.md`（项目根目录），共 39 项问题 + UI/功能建议。
- 最高优先级发现（已逐行核实源码）：
  1. Critical: `indexedDBAdapter.ts` 在 db 未就绪时 `if(!this.db) return` 静默丢写；`main.tsx:14` 同步调用 `migrateLocalStorageToIndexedDB()` 在 whenReady 前执行 → 升级用户记录跨重启丢失。
  2. High: `DrawerNewRecord.tsx` 草稿自动保存 effect 依赖 `changeCountRef.current`（ref 不触发重渲染）→ 草稿永不保存；Dashboard/DataVolumeGauge/DeadlineWarning/NotificationPanel 挂载即读空缓存且 `useMemo([],)` 不刷新；Backup.tsx:70 仍读 localStorage 的 `jingzong.mass.records`。
- 跨模块问题：三套搜索 UI（GlobalSearch 未挂载/CommandPalette/FloatingSearch）、法定时限规则 3 处定义且字段分歧、moduleConfig.ts 1456 行上帝文件+案件类型复制 12 次、两套设计语言撞车。
- 方法：3 个并行 Explore 子代理分维度分析 + 主代理逐行核实 Critical/High。

## 按审查结论推进修复（除登录页"赛博朋克战术玻璃" V1 不动）
用户指令：除主系统"政务蓝" vs 登录页"赛博朋克战术玻璃"这一项不动，其余按结论修复/改进/推进。

### 已完成（本轮 + 前序轮次）
- Critical C-1/C-2 + M-3：IndexedDB 写入丢写 + 迁移时机修复（whenReady 前不写、升级迁移延后）。
- High：H-1 DrawerNewRecord 草稿保存 ref→state；H-5 NotificationPanel 接 useDataChanged 重读；H-6 Backup getRecordStats 改读 IndexedDB + 重置清附件；H-7 CaseDetail 合并双扫描；H-8 抽取统一 `constants/fieldLabels.ts`（513 键，三文件以 `...SHARED`+覆盖保留原标签）。
- M-1：electron/preload.cjs 全部 `on*` 返回退订函数 + useReminderService/DailyNotes/Backup 清理；M-2 重置清附件；M-4 导入 JSON 后 notifyDataChanged；M-5 抽取共享 `components/EChartBox.tsx`（init 一次 + setOption 合并），Statistics/Dashboard 接入；M-8 ModulePage rows/dynamicColumns/dataFields 加 useMemo；M-9 CaseGraph 搜索拆独立 effect（仅透明度合并）+ 250ms 防抖，避免每次按键重建力导向图。
- Low：L-1 ModulePage 删除死代码 sectionFields；L-2/L-3 Version.tsx 扩展 ParsedEntry 类型含 增强/其他 + changelog 正则扩类型集并兜底"其他"，统计卡片不再 undefined；L-7 OperationLog 加 DEFAULT_TYPE_META 防 undefined；L-8 Statistics 聚合 memo；L-10 GlobalSearch 记录列表 memo 一次，底部不再重复 getMassRecords()；L-12 新增 massStore.getMassRecordById 并替换 Dashboard/DeadlineWarning 的散落 find；L-13 main.cjs 删除重复 note-drag handler。

### 刻意跳过（风险/范围）
- M-7 ModulePage/CaseTimeline 的本地 displayValue/fmtTime：共享 formatValue **不识别 ISO 日期串**，直接替换会回归日期显示，故保留本地格式化函数。
- M-10 模块标签统一、M-11 reportUtils/reportGenerator 合并、M-14 electronAPI 抽到 electron.d.ts、C-M2/C-M7/C-M8 跨模块重构：体量大/改动面广，按"高风险重构先不动"原则跳过。
- L-4/L-6/L-11/L-14/L-15：可选/高风险，跳过。
- V1 登录页战术玻璃 UI：用户明确要求不动，全程未触碰（index.css:458-533、LoginPage.tsx:387 注入保持原样）。

### 验证
- `npm run typecheck`（tsc --noEmit --skipLibCheck）通过，0 错误。
- `npm run test`（vitest run）：17 tests 通过（2 文件）。注：出现 `indexedDB is not defined` 未处理 rejection —— 系测试环境无 indexedDB 的既有问题，非本次改动引入，且测试均通过。
- 临时脚本 tmp_merge_fieldlabels.cjs / tmp_ct_fieldlabels.cjs 已清理。

## 继续推进「跳过项」（本轮）

用户指令"继续跳过项"。此前安全子集（M-7/M-10/M-11/M-14/C-M2/C-M8/L-4）已完成。本轮落地剩余 deferred 项中的安全子集：

- **L-6 侧边栏 URL 驱动导航（已修复）**：根因——`AppLayout` 已有 `location.pathname → setCurrentPage` 同步，但 Sidebar 仅 `setCurrentPage(id)` 从不 `navigate`，导致刷新恒回 dashboard、书签/前进后退失效。修复方案：在 `AppLayout` 增加「URL↔currentPage 双向同步」两个 `useEffect`（URL→state 恢复刷新/书签/历史；state→URL 同步地址栏），不改动约 10 处 `setCurrentPage` 调用方，避免回环（故意省略 `currentPage` 进 URL→state effect 依赖）。刷新/书签/浏览器历史现已可用。
- **L-14 骨架屏（已修复）**：新增 `src/components/PageSkeleton.tsx`，复用 `index.css` 中此前未使用的 `.skeleton/.skeleton-title/.skeleton-text/.skeleton-card` 工具类，替换 `AppLayout` 的 `<Suspense fallback={Spin}>` 为 `<PageSkeleton/>`（路由懒加载占位反馈）。
- **C-M7 版本命名（已修复）**：`src/store/versionStore.ts` → `appVersionStore.ts`（git mv 保留历史，仅 `Version.tsx` 一个引用方已同步）；修正 `CommandPalette.tsx` 把 `setCurrentPage` 别名成 `navigate` 的误导写法（replace_all 重命名，约 10 处调用 + 依赖数组）。
- 验证：`npm run typecheck` 0 错误；`npm run test` 17/17 通过（1 个既有 `indexedDB` 测试环境报错，无关）。无残留 `versionStore`/`Spin`/`navigate` 别名引用。

### 仍暂缓（需单独规划 + 用户授权）
- **L-11** echarts tree-shaking（3 处 `import * as echarts` → 按需引入）；**L-15** hover 统一（whileHover + 条件式 onMouseEnter 散落 ~15 文件，强统一会丢行为）。已建任务 #22 跟踪。
- **M-6/M-12/M-13/M-15/M-16** 架构级重构（moduleConfig 上帝文件拆分、存储统一、appStore 拆分、any 收敛、data.ts 删除）——高风险，按"不盲动"原则保持 pending（任务 #19）。
- V1 登录页战术玻璃 UI：仍按用户要求全程未触碰。
