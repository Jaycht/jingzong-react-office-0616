# 2026-07-19 工作日志

## V2.37.0 调证登记案件/线索切换体验 + 全局数据共享
- **需求（用户三点）**：①切换按钮更醒目；②线索调证四项应与案件调证四项一样「全局数据共享」；③确认是否"无论案件/线索调证前面四项都加载到同一记录"。
- **第3点澄清**：此前实现是两套独立字段（caseNo/caseName/caseSource/caseType 与 clueNo/clueName/clueSource/clueType）、切换会清空，并未统一。用户选「保留两套+全局联想(推荐)」方案。
- **改动**：
  - `DrawerNewRecord.tsx`：切换控件由灰色 Segmented 改为带 2px 主色边框横幅 + 大号实色 Radio.Group（案件调证/线索调证）+ 状态 Tag；`handleModeChange` 去掉清空另一侧逻辑（切换不清空已填值）；提交时(`handleSubmit`)按 requestMode 删掉非当前模式的四项，记录仍只含一套字段；clueNo 改用新组件 `GlobalClueNoField`。
  - `SharedFormFields.tsx`：新增 `GlobalHistoryField`（按 field.id 独立全局历史池，用于 clueName/clueSource）与 `GlobalClueNoField`（XS- 前缀强制 + clueNo 全局历史联想）。
  - `evidence.ts`：`caseSource` 补 `customOptionKey:'evidence.request.caseSource'`（经 PersistedSelect 全局持久化自定义来源项）；caseType/clueType 此前已配 key。
  - 分支逻辑：`DynamicField` 中 clueName 不再借 `GlobalCaseNameField`（案件名称池），改 `GlobalHistoryField`（clueName 独立池），与案件名称池互不干扰。
- **校验**：typecheck 0 错；test 57 全过；build 成功。
- **提交**：`git commit` 分支 `snapshot/v2.14.50-preplan`（未 push），涉及 version.ts/package.json/evidence.ts/SharedFormFields.tsx/DrawerNewRecord.tsx。
- **注意**：clueSource 本就是 text→InputWithHistory（自带 clueSource 全局池）、caseType/clueType 本就 PersistedSelect 全局，本次主要是补齐 clueName 独立池、clueNo 历史、caseSource 全局键。

## 经侦 77 类司法解释"搜集全面"调研（用户要求尽量补全）
- 结论：77 类主要靠《规定（二）》(已在库) 统领；真正有专门司法解释的类型有限，库内 33 部司法解释已覆盖多数高频罪名（假币/非法集资/内幕交易/利用未公开信息/操纵证券期货/骗购外汇/洗钱/税收13项/传销/虚假诉讼/信用卡/非法经营部分）。
- **确缺、建议补的专门司法解释只有 2 部（官方现行有效）**：①《关于办理走私刑事案件适用法律若干问题的解释》法释〔2014〕10号 → 第2类走私假币；②《关于办理商业贿赂刑事案件适用法律若干问题的意见》法发〔2008〕33号 → 第10/11/12类。
- 重要核实：欺诈发行证券/违规披露/不披露重要信息 **2022年司法解释备案目录无专门刑事解释**（仅民事《虚假陈述侵权赔偿规定》法释〔2022〕2号已入库），两类靠《规定（二）》即可；逃汇(190)无专门解释（1998骗购外汇解释仅共犯）；骗取贷款(175之一)草案曾撤回无专门。
- 可选补高价值纪要：全国法院审理金融犯罪案件工作座谈会纪要 法〔2001〕8号（金融诈骗非法占有目的）、涉互联网金融犯罪纪要 高检诉〔2017〕14号。
- 交付：`.import_tmp/经侦77类司法解释对照与补库清单.md`（77类↔司法解释完整对照表+必补/可选清单+国家法律法规数据库下载步骤）。待用户下载那 2 部 docx 后按此前 python-docx 流程入库。

## V2.38.0 补入商业贿赂意见（走私解释待补）
- 用户发来 2 个 docx 引用，但 `D:/下载/` 下用 `*走私*` 通配**无任何匹配、也无 Word 锁文件** → 走私解释那份实际未就位（用户说"下载好了"但文件不在），属待确认项，按用户习惯直接上报等其确认/重发，不臆测路径。
- 商业贿赂意见（`关于办理商业贿赂刑事案件适用法律 若干问题的意见.docx`）抽取成功：2130 字符、法发〔2008〕33号、2008-11-20 印发、共 11 条意见、覆盖刑法163/164（非国家工作人员受贿/对非国家工作人员行贿等）；"意见"结构无"第X条"，articles=0 整篇渲染。
- 入库：复制为 `public/laws/司法解释/关于办理商业贿赂刑事案件适用法律若干问题的意见.txt`（规范文件名去中间空格、加标准头 `# 标题`+`> 来源/原文链接`）；manifest 追加条目；司法解释 33→34、totalLaws 78→79。
- EC_RELATED 新增 10/11/12 → 该意见 id；校验 14 个引用全部存在于 manifest ✓。
- 升版 V2.38.0（version.ts+package.json），CHANGELOG 注明走私解释待补。
- 校验：typecheck 0 错；test 57 全过；build 成功（法条随包内联）。
- 提交：`snapshot/v2.14.50-preplan`（未 push）。走私解释到后再升一版单独入（含第2类 EC_RELATED 关联）。

## V2.39.0 补入走私解释（「必补2部」全部补齐）
- 用户重新发来 `D:/下载/最高人民法院、最高人民检察院关于办理走私刑事案件适用法律若干问题的解释_20140812.docx`（42KB，10:03 下载），本次确认就位。python-docx 抽取：14517 字符、法释〔2014〕10号、2014-08-12 公布、2014-09-10 施行、共 25 条（正确的"第X条"结构，阅读器可逐条渲染），末段为走私珍贵动物附录名录。
- 入库：`public/laws/司法解释/关于办理走私刑事案件适用法律若干问题的解释.txt`（加标准头）；manifest 追加条目（articles=25、timeline 含公布+施行两节点）；司法解释 34→35、totalLaws 79→80（计数 80/80/80 一致）。
- EC_RELATED 新增第 2 类 → 该解释 id（覆盖经侦77类第2类走私假币）；校验 15 个引用全部存在于 manifest ✓。
- 升版 V2.39.0（version.ts+package.json），CHANGELOG 注明「必补2部」至此全部补齐。
- 校验：typecheck 0 错；test 57 全过；build 成功（法条随包内联）。
- 提交：`snapshot/v2.14.50-preplan`（未 push）。至此对照清单「必补2部」已全入库，经侦77类专项司法解释关联完整（含走私假币第2类）。

## V2.40.0 系统设置页栏目精简与默认头像确认
- 用户要求：系统设置里栏目太散，除「关于软件」不动外，合并/删除；并确认默认头像是否仍为警徽。
- 默认头像：机制仍在。`AppLayout.tsx:143-144` 与 `ProfileModal.tsx:236` 均使用 `avatarSrc = customAvatar || "/avatar-default.jpg"`，且 `public/avatar-default.jpg` 文件内容即为警徽图（蓝盾金色国徽）。
- 栏目优化（系统设置页左侧 tab 从 7 个减为 5 个）：
  - 合并「导入导出」「备份恢复」为「数据管理」单一栏目；创建 `src/pages/DataManagement.tsx`，内嵌 `ImportExport` + `AutoBackupPanel` + `Backup`。
  - 将「自动备份与存储路径」从「通用设置」底部迁移到「数据管理」内，避免跨栏目分散。
  - 删除单机场景下低频的「操作日志」栏目；导出审计日志仍保留在数据管理-导入导出中。
  - 保留「通用设置」「模块与字段」「附件档案」「关于软件」。
- 通用设置内部分组合并：由 5 个 Section + 1 面板 合并为 3 个 Section（系统外观与行为、声音与提示、列表与偏好），更紧凑。
- 为 `ImportExport.tsx` 和 `Backup.tsx` 增加 `noHeader?: boolean` prop，在 DataManagement 中嵌入时不重复渲染页面标题。
- 升版 V2.40.0（version.ts + package.json），CHANGELOG 首项已记录。
- 校验：typecheck 0 错；test 57 全过；build 成功。
- 提交：分支 `snapshot/v2.14.50-preplan`（未 push）。

## V2.41.0（顶栏性能双态 + 删除头像）
- 用户要求：①顶栏「性能」按钮改为高/低两种形态，能一眼看出当前模式；②个人资料（更换头像）加「删除头像」功能，位置由我定。
- 性能按钮（`AppLayout.tsx` 顶栏右侧）：原为恒显「性能」文案、点击仅 toggle `lowPerfMode` 无可见反馈。现改为直接显示当前模式文案（高性能 / 低性能）+ 状态色点（高性能=绿 #10B981、低性能=琥珀 #F59E0B），`Gauge` 图标也随模式变色；title 提示当前模式与点击将切换的目标。状态仍走 `uiSlice.lowPerfMode`（false=高性能默认、true=低性能），持久化 localStorage `jingzong.lowPerfMode` 不变。
- 删除头像（`ProfileModal.tsx`）：头像下方新增「更换头像 / 删除头像」按钮行。删除处理函数 `handleDeleteAvatar` 经 `Modal.confirm` 二次确认后 `localStorage.removeItem("jingzong.avatar")` + `setAvatar(null)` + 置 dirty；仅在已设自定义头像时按钮可点，默认头像（警徽）时禁用并灰显，避免误删。头像预览即时回退警徽，关闭弹窗后 AppLayout 顶栏头像随重渲染同步更新（沿用既有「改头像即写 localStorage」机制）。
- 升版 V2.41.0（version.ts + package.json），CHANGELOG 首项已记录。
- 校验：typecheck 0 错；test 57 全过；build 成功。提交 `5eaeb08`（分支 snapshot/v2.14.50-preplan，未 push）。

## V2.41.4（新建弹窗蓝头 CSS 强制去边距）
- 用户反馈：V2.41.3 仍有上/左/右白边。
- 原因：`styles.content: { padding: 0 }` 被 antd Modal 默认 `.ant-modal-content` 样式优先级覆盖，导致内联样式不生效。
- 处理：
  - 给 `DrawerNewRecord.tsx` 的 Modal 加 `className="drawer-new-record-modal"`；
  - 在 `src/index.css` 新增作用域规则：
    ```css
    .drawer-new-record-modal .ant-modal-content { padding: 0 !important; }
    .drawer-new-record-modal .ant-modal-footer { padding: 12px 24px 24px !important; margin: 0 !important; }
    ```
  - 修复编辑 `index.css` 时意外破坏 `.hover-bg-primary:hover` 的语法错误（保留原规则）。
- 升版 V2.41.4（version.ts + package.json），CHANGELOG 首项已记录。
- 校验：typecheck 0 错；test 57 全过；build 成功。提交 `98ad01a`（分支 snapshot/v2.14.50-preplan，未 push）。

## V2.41.8（深色表单统一真正生效 + 新建弹窗加警徽图标）
- 用户反馈：①深色模式下仍有输入框白底；②新建弹窗头部太素，想在标题前加警徽图标。
- 处理：
  - `src/index.css`：V2.41.7 的规则对 antd 6 css-in-js 生成的 variant 类 specificity 不够，追加 `.ant-form` 作用域的高优先级规则（覆盖 `.ant-input-outlined/.ant-input-filled/.ant-input-borderless` 及 AutoComplete/Select/DatePicker/InputNumber/Mentions），统一背景/文字/边框/placeholder/hover/focus；修复追加规则时意外破坏低性能模式注释块导致的 CSS 语法错误。
  - `src/components/DrawerNewRecord.tsx`：蓝渐变标题区在标题文字前加 `<img src="/badge-icon.png" />`（24×24，object-fit: contain），并调整标题行 flex 间距。
  - 将 `D:/下载/2.png` 复制到 `public/badge-icon.png` 作为项目资源。
- 升版 V2.41.8（version.ts + package.json），CHANGELOG 首项已记录。
- 校验：typecheck 0 错；test 57 全过；build 成功。提交 `a801012`（分支 snapshot/v2.14.50-preplan，未 push）。

## V2.41.7（深色模式统一表单控件样式）
- 用户反馈：深色模式下新建弹窗内有的输入框是白底（Input），有的下拉框/日期选择器是深底，风格不统一。
- 处理：`src/index.css` 在「深色模式：Ant Design 组件覆盖」区追加统一规则，覆盖：
  - Input / InputNumber / Select selector / DatePicker 输入框：背景统一为 `--color-surface`，文字 `--color-text`，边框 `--color-border`；
  - placeholder 统一为 `--color-text-muted`；
  - hover / focus 统一高亮主色并加蓝色光晕；
  - Select 下拉浮层、DatePicker 弹层面板统一深底 + 阴影；
  - 下拉项、日期格子、表头、选中态、今天标识统一配色。
- 升版 V2.41.7（version.ts + package.json），CHANGELOG 首项已记录。
- 校验：typecheck 0 错；test 57 全过；build 成功。提交 `7c0529c`（分支 snapshot/v2.14.50-preplan，未 push）。

## V2.41.6（蓝渐变头顶部恢复圆角）
- 用户反馈：V2.41.5 铺满后顶部变成直角，不好看。
- 原因：清零 `.ant-modal-container` 的 padding 后，Modal 的圆角依赖 container 内边距撑出；padding 为 0 时内容区直接顶到容器直角边界，顶部圆角消失。
- 修复：`DrawerNewRecord.tsx` 蓝渐变头 div 增加 `borderRadius: '8px 8px 0 0'`，让蓝头自身携带顶部圆角，与弹窗外框圆角一致；其他铺满逻辑不变。
- 升版 V2.41.6（version.ts + package.json），CHANGELOG 首项已记录。
- 校验：typecheck 0 错；test 57 全过；build 成功。提交 `7698792`（分支 snapshot/v2.14.50-preplan，未 push）。

## V2.41.5（根因修复：antd6 容器为 .ant-modal-container）
- 用户反馈：V2.41.4 仍和之前一样有上/左/右白边，无任何变化。
- 真正根因：antd 6 已**移除 `.ant-modal-content` 这一层**（旧版 Modal 才有），白边实际来自 `.ant-modal-container` 的 `contentPadding`（`paddingMD paddingContentHorizontalLG` ≈ 16px 24px）。V2.41.3/41.4 针对 `.ant-modal-content` 的选择器全部未命中，所以一直无效。
- 修复：
  - `DrawerNewRecord.tsx` 的 Modal 改用 `classNames={{ container: 'dnr-modal-container' }}`（antd 6 的 `classNames` 支持 root/header/body/footer/container/title/wrapper/mask/close，但**没有 content**），去掉无效的 `styles.content`；
  - `src/index.css` 把选择器从 `.drawer-new-record-modal .ant-modal-content` 改为 `.dnr-modal-container { padding: 0 !important }`，footer padding 覆盖保留；
  - 蓝头 div 自身 padding 保持 `16px 24px 20px`，背景色现在从弹窗内容区最上/最左/最右开始，四周无白边。
- 升版 V2.41.5（version.ts + package.json），CHANGELOG 首项已记录。
- 校验：typecheck 0 错；test 57 全过；build 成功。提交 `3dc4181`（分支 snapshot/v2.14.50-preplan，未 push）。
- 经验（重要）：antd 6 的 Modal 容器节点是 `.ant-modal-container`（内容白卡），不是旧版 `.ant-modal-content`；任何想去掉 Modal 内边距/改背景的样式，必须用 `classNames.container` 加专属类来覆盖，旧选择器无效。后续其他弹窗（如 DrawerNewRecord 之外的 Modal）如需同样改造，复用此法。

## V2.41.3（新建弹窗蓝头去边距铺满）
- 用户反馈：V2.41.2 的蓝渐变头虽然从标题延伸到基本信息上方，但四周仍有白色间隙（Modal content 默认 padding 导致），要求真正铺满上下左右。
- `DrawerNewRecord.tsx`：将 `Modal` 的 `styles` 扩展，增加 `content: { padding: 0 }` 以清除 `.ant-modal-content` 默认内边距；`footer` 单独补 `padding: '12px 24px 24px'`，避免按钮贴边；`body` 保持 `padding: 0` + flex 列布局。
- 蓝头 div 本身仍保留 `padding: '16px 24px 20px'`，因此内容不会贴到弹窗边缘，但蓝色背景现在从弹窗内容区最顶部/最左/最右开始，四周无白边。
- 升版 V2.41.3（version.ts + package.json），CHANGELOG 首项已记录。
- 校验：typecheck 0 错；test 57 全过；build 成功。提交 `58b58ab`（分支 snapshot/v2.14.50-preplan，未 push）。

## V2.41.2（新建弹窗头部蓝渐变上下铺满）
- 用户反馈：新建弹窗蓝渐变只铺了标题条，没有铺满到「基本信息」上方；而且渐变是左右方向，希望像资料弹窗头部那样上下渐变并铺满截图中的红色框区域。
- `DrawerNewRecord.tsx`：把标题栏、步骤条、模块/模板选择器（含面包屑、当前步骤标题「基本信息」、所属模块下拉、记录模板下拉）统一放进一个蓝色渐变横幅内，从弹窗顶部一直铺到「基本信息」上方；下方字段区恢复白底。
- 渐变方向改为上下：`linear-gradient(to bottom,#155A8A,#2563EB)`（浅色模式）、`linear-gradient(to bottom,#13325c,#1d4ed8)`（深色模式），与资料弹窗横幅一致。
- 横幅内文字/面包屑/字段标签全部改为白或半透明白；模块选择器卡片用 `rgba(255,255,255,0.10)` 背景 + `rgba(255,255,255,0.18)` 边框，保持可读又有层次；下拉框本身白底，在蓝头上形成清晰控件。
- 升版 V2.41.2（version.ts + package.json），CHANGELOG 首项已记录。
- 校验：typecheck 0 错；test 57 全过；build 成功。提交 `9367866`（分支 snapshot/v2.14.50-preplan，未 push）。
- 预览：http://localhost:5173/ （dev server 仍在运行，HMR 已热更新）。


- 用户意见：主界面全是白色太素，建议把标题栏背景改成资料弹出头像区的渐变蓝背景色。评估可行并配套改造。
- `AppLayout.tsx` 顶栏换肤：浅色模式 `app-topbar` 背景由纯白 `#fff` 改为蓝渐变 `linear-gradient(135deg,#155A8A,#2563EB)`（与 `ProfileModal.tsx:164` 资料弹窗横幅同款）；深色模式同步统一为 `#13325c→#1d4ed8`（原近黑 `#0e1626`）。Electron 标题栏 `electron-titlebar` 背景也改为同款渐变、去掉分隔线，使整个顶部视觉统一。
- 顶栏内元素配套转浅色（原按 darkMode 分深色字，现顶栏恒为深色底统一用浅色）：标题「经侦工作记录」/ 用户名 / 部门 → 白字 + 白透；四个按钮（资料/深色/性能/退出）经 `topBtn()` 统一白字 `rgba(255,255,255,0.92)` + 白色半透明描边 `rgba(255,255,255,0.28)`；退出按钮红字 `#DC2626` 改浅红 `#FCA5A5`；logo 方块由蓝渐变改白底蓝字 `#1d4ed8` 以在蓝顶上突出。
- 全局搜索框（GlobalSearch）保留白底不动：蓝底衬白框对比清爽，无需改。
- `src/index.css` 新增作用域规则 `.app-topbar .wb-hover-ghost:hover`（仅顶栏内生效，不影响其他页面），hover 时背景 `rgba(255,255,255,0.14)` + 描边更亮 + 文字白。
- 升版 V2.41.1（version.ts + package.json），CHANGELOG 首项已记录。
- 校验：typecheck 0 错；test 57 全过；build 成功。提交 `758450a`（分支 snapshot/v2.14.50-preplan，未 push）。
- 预览：http://localhost:5173/ （dev server 仍在运行，HMR 已热更新）。

## 工作台顶部欢迎区加实时时钟（不迭代版本号/更新日志，用户明确）
- 需求：工作台"警号 123456 · 资金组 · 2026年7月19日星期日"日期后加当天设备时间，精确到秒。
- 改动：`src/pages/Dashboard.tsx` 新增顶层 `HeroClock` 组件（独立 useState+setInterval 每秒刷新，隔离重渲染），在第313行 `dash-hero-sub` 处追加 ` · <HeroClock />`；并补 `useState/useEffect` 的 react import。
- 校验：typecheck 0 错；build 成功（2.56s）。不提交版本号/CHANGELOG（按用户要求）。dev server HMR 已热更新，预览 http://localhost:5173/。

## 项目收尾审查（用户要求查错误/冗余/占用空间/可精简，仅审查未改动）
- 磁盘·零引用可清理：`w700d1q75cms.jpg`(根,336K,AI图残留)、`$null`(根,0B,命令残留)、`stitch_vibrant_smart_worksphere/`(5.1M空壳)、`public/images/`6图(bg/head_bg/jt/lbx/line/map,732K)、`public/fonts/DS-DIGIT.TTF`(25K)、`.import_tmp/`(3M法规导入临时脚本,导入已完成)、根散文档(notification_preview.html/note_preview.html/两份法规搜集清单md/CODE_REVIEW_REPORT.md/部门模块体检报告.md)。
- ⚠️ 必须保留·勿误删：`audio/`(11M,提醒铃声音频,uiSlice.ts `new Audio("/audio/"+name)`+electron/notification.html 引用,V2.14.3功能)、`build/installer.nsh`(package.json electron-builder include 引用)、`app.ico`、`public/avatar-default.jpg`、`public/badge-icon.png`、`public/laws`、`public/forms`。
- 代码死代码(零引用)：`src/constants/caseTypes.ts`(被 moduleConfig/caseTypes.ts 取代)、`src/utils/format.ts` 的 formatDate/formatDateTime、`src/constants/theme.ts` 的 KPI_GRADIENTS/CHART_COLORS/STAT_COLORS/KPI_ENTRY_GRADIENTS。
- 可选优化(有改动风险,待拍板)：Modal.confirm 静态方法~13处不继承主题→改 App.useApp()；ModulePage Table 补 rowKey="id"；动态列表 index key 改稳定 id；isElectron 内联6+处统一 import；reportUtils/reportGenerator 合并转义/日期到 htmlUtils/format；EmptyState 通用化。

## V2.41.13 收尾清理（执行审查清单「可选优化」全部项）—— ✅ 已完成
- 用户拍板「C. 全部都做（含 Modal 主题统一）」，本版落地：
  1. **磁盘残留清理**（PowerShell 物理删除，已核对保留项）：`w700d1q75cms.jpg`/`$null`/`notification_preview.html`/`note_preview.html`/两份法规搜集清单.md/`CODE_REVIEW_REPORT.md`/`部门模块体检报告.md`/`stitch_vibrant_smart_worksphere/`(5.1M)/`.import_tmp/`(3M)/`public/images/`6图/`public/fonts/DS-DIGIT.TTF`；`src/constants/caseTypes.ts` 整文件（被 moduleConfig/caseTypes.ts 取代）。**保留项确认未删**：`audio/`(11M)、`build/installer.nsh`、`app.ico`、`public/avatar-default.jpg`、`public/badge-icon.png`、`public/laws/`、`public/forms/`。
  2. **死代码删除（零引用）**：`src/utils/format.ts` 删 formatDate/formatDateTime；`src/constants/theme.ts` 删 KPI_GRADIENTS/CHART_COLORS/STAT_COLORS/KPI_ENTRY_GRADIENTS。
  3. **Modal 静态方法统一为 App.useApp()（主题生效关键）**：根 `App.tsx` 加 `<AntApp>` 包裹（ConfigProvider 内）；`AppLayout/DailyNotes/ModulePage/DrawerNewRecord/Backup/ProfileModal` 共 14 处 `Modal.confirm/warning/...` 改为 `App.useApp()` 返回的 `modal` 实例。修复暗色/品牌色在静态弹窗不生效的问题。
  4. **rowKey 与动态列表稳定 key**：ModulePage 表格补 `rowKey="key"`（与卡片视图 `key={row.key}` 一致，=rec.id）；Dashboard 最近动态补 `id:r.id` 改用 `key={a.id}`；OperationLog 改用 `key={log.id}`（列表项自带 id）。判断保留项：DailyNotes `contents` 为 `string[]` 受控输入框，改稳定 id 需重构数据模型+持久化+导入导出（中风险、收益低），保持 `key={i}`；CaseGraph 案件名为只读展示，按既定原则留 index。
  5. **isElectron 内联判断统一收敛**：`AppLayout/LoginPage/SystemSettings/DailyNotes/useUnsavedChanges/attachmentStore` 6 处散落 `typeof window!=='undefined' && window.electronAPI?.isElectron`（DailyNotes 原检查 `?.createNoteWindow`，语义等价为 Electron 专属，一并改 `isElectron()`）统一改为 `import { isElectron } from '../lib/env'`。`attachmentStore` 原私有 `function isElectron()` 删除、复用 lib/env。全局仅 `src/lib/env.ts` 保留唯一定义。
  6. **报告模块重复逻辑合并**：`reportGenerator.ts` 删私有 `fmtDate`，改用 `format.formatChineseDate`（输出 `YYYY年M月D日` 一致）；`reportUtils.ts` 的 `dateStr` 同样改 `format.formatChineseDate(new Date())`，去除重复的日期格式化。
- **升版**：`src/version.ts` V2.41.12→V2.41.13（patch+1，清理），CHANGELOG 首项说明上述收尾优化；`package.json` 同步 2.41.13。
- **校验**：`npm run typecheck` 0 错；`npm run test` 57 全过（act 警告为既有非致命）；`npm run build` 成功（rolldown，仅 LegalKnowledge 大包体积预警，非回归）。
- **未做项（用户清单内，本次未触及，仍可选）**：`EmptyState` 通用化（原清单末项，属纯体验无关风险，未做）。

- **Git 同步状态（2026-07-19）**：V2.41.13 已全部 commit（102 文件，含上轮清理物理删除残留 + 死代码删除），提交在本分支 `snapshot/v2.14.50-preplan`。
  - **关键网络经验**：本 WorkBuddy Bash 进程**直连 github.com:443 会超时**（沙箱内外都试过），因为 git 不走系统 VPN 代理。探测发现本机代理监听 `127.0.0.1:7890`（curl 经它访问 github 返回 200）。已 `git config http.proxy http://127.0.0.1:7890` + `https.proxy`（**仅本仓库 --local**，不动全局）。
  - **GitHub 已推送成功**：`git push origin HEAD:refs/heads/master` → `1cc1782..a96f860`（fast-forward，V2.41.13 已同步到 origin/master）。
  - **VPN 关闭后恢复直连**：`git config --unset http.proxy && git config --unset https.proxy`。
  - **腾讯云 CODING**：用户要求也推到 CODING 并「云端 CI 构建」，但用户未提供仓库 HTTPS 地址（仅选了"粘贴 HTTPS 地址"选项却没贴 URL），待用户给 `https://e.coding.net/<团队>/<项目>/<仓库>.git` 后：加为 `coding` remote 推送 + 加 Jenkinsfile（npm install → npm run build；打 exe 需 Windows 构建节点）。CODING 推送同样需走 7890 代理。
  - **2026-07-19 15:0x 用户改主意**：不推 CODING，改为 **GitHub 上构建打包**。项目已有 `.github/workflows/build.yml`（V2.9.11 起就在）：触发 `tags: v*` 或 `workflow_dispatch`；`windows-latest` 运行器；`npm install --legacy-peer-deps`（带 `ELECTRON_MIRROR=https://npmmirror.com/mirrors/electron/`）；`npm run electron:build`（= typecheck + vite build + electron-builder 出 nsis）；若是 tag 触发则 `action-gh-release` 自动建 Release 并把 `dist/*-V*.exe` 挂上。**操作方式**：`git tag -a v2.41.13 -m "..." HEAD && git push origin refs/tags/v2.41.13` 即触发构建（约几分钟）。已成功推送 tag v2.41.13，构建在 GitHub 云端跑。查看：Actions `https://github.com/Jaycht/jingzong-react-office-0616/actions`；Release `https://github.com/Jaycht/jingzong-react-office-0616/releases/tag/v2.41.13`。注：`gh` CLI 在本机是坏掉的 Python 包装脚本（bad interpreter），且匿名 API 受代理出口 IP 限流，无法在此轮询状态，需用户在网页看。

- **V2.41.14 修复（2026-07-19 15:3x）**：用户反馈 9 项问题集中修复：
  1. 取消离线版自动更新弹窗（App.tsx 移除检查 effect；main.cjs 设 `autoInstallOnAppQuit = false`）。
  2. 顶栏全局搜索遮挡头像 → AppLayout 改为 `grid-template-columns: 1fr minmax(0,720px) 1fr` 居中且不重叠；Electron 标题栏控制按钮 `justify-content:flex-end` 移到最右侧。
  3. 桌面便签重设计（electron/note.html）：24px 真圆角、玻璃拟态背景、更柔和阴影、SVG 图标替换 emoji、边框与内发光强化圆角真实感。
  4. DailyNotes 编辑 Modal 报 `Modal is not defined` → 补回 antd `Modal` import（V2.41.13 Modal 重构漏改 JSX）。
  5. 备份默认文件名含冒号导致 Windows 提示无效 → excelUtils.ts 文件名替换 `:` 为 `-` 并过滤 `< > : " / \\ | ? *`。
  6. 案件时间轴深色下拉菜单白色 → 原生 `<select>` 改为 antd `Select`，dropdown 随 ConfigProvider 深色主题；index.css 同步样式。
  7. 文书库 PDF/Word 假下载 → LegalForms.tsx 改为 `fetch(blob) + file-saver` 真正保存到磁盘，并过滤文件名非法字符。
  8. 新建弹窗警徽图标丢失 → `/badge-icon.png` 改为内联 SVG 警徽，避免打包后资源路径问题。
  9. 校验：typecheck 0 错 / test 57 全过 / build 成功；版本 V2.41.14，tag 已推送 GitHub 触发 Actions 重新打包。查看：Actions https://github.com/Jaycht/jingzong-react-office-0616/actions ；Release https://github.com/Jaycht/jingzong-react-office-0616/releases/tag/v2.41.14。

- **V2.41.15（2026-07-19 16:0x）用户追加 4 项反馈修复**：
  1. 窗口化时头像被挤压 → AppLayout 顶栏右侧个人信息区加 `min-width: fit-content` + `flex-shrink: 0`，避免被中间搜索区压缩。
  2. 桌面便签假圆角（尤其底部两角）→ `electron/main.cjs` 便签窗口显式 `roundedCorners: true`；`electron/note.html` 增加 `.note-shell` 阴影层并将 `.note` 加 `clip-path: inset(0 round 24px)` 做最终裁剪保险，确保圆角真实。
  3. 新建弹窗自绘警徽太丑 → 改回用户提供的 `D:\下载\2.png`（内容同项目 `public/badge-icon.png`），`DrawerNewRecord.tsx` 引用 `/badge-icon.png`。
  4. 系统设置新增「关闭窗口时的行为」三选一（直接退出 / 最小化到托盘 / 每次询问），保存于 `jingzong.closeBehavior`；`App.tsx` 启动时同步给 Electron 主进程；`electron/main.cjs` close 事件按设置处理，询问模式用原生 `dialog.showMessageBoxSync` 弹窗选择。
  校验：typecheck 0 错 / test 57 全过 / build 成功；版本 V2.41.15 已 push + tag，Actions 正在构建。查看：Actions https://github.com/Jaycht/jingzong-react-office-0616/actions ；Release https://github.com/Jaycht/jingzong-react-office-0616/releases/tag/v2.41.15。

- **V2.41.16 修复（2026-07-19 16:3x）**：用户反馈 V2.41.15 四项均未真正生效，定位根因并修复：
  1. 关闭按钮未读取设置 → `AppLayout.tsx` 的 `winControls.close` 原来硬编码 `modal.confirm` 直接弹出「确认退出」，把主进程按设置处理的行为覆盖掉了。现移除该弹窗，关闭按钮直接调用 `window.electronAPI.close()`，由 `electron/main.cjs` 的 `close` 事件按 `jingzong.closeBehavior`（exit/tray/ask）处理。系统设置里的「关闭窗口时的行为」选项本身已在 V2.41.15 加入，现在设置真正生效。
  2. 新建弹窗警徽不显示 → 原因：Electron 生产包中绝对路径 `/badge-icon.png` 解析为 `file:///badge-icon.png`（而非 dist 根目录），导致 404。修复：将 `public/badge-icon.png` 复制到 `src/assets/badge-icon.png` 并通过 Vite 模块导入 `import badgeIcon from '../assets/badge-icon.png'`，构建后文件进入 `dist/assets/` 并带 hash，路径正确。
  3. 窗口化头像仍被挤压 → 原因：顶栏 grid 中间列固定 `minmax(0, 720px)`，在窗口窄时优先占满 720px，右侧 1fr 被压缩。修复：grid 改为 `minmax(0, 1fr) minmax(0, 1fr) auto`，中间搜索框保留 `maxWidth: 720`，右侧列按内容宽度自适应，全局搜索随窗口化自动收缩。
  4. 便签下面圆角、上面假圆角 → 原因：`.header` 使用 `-webkit-app-region: drag`，Windows 会把整个 header 区域视为非客户区，系统强制顶部为直角，覆盖窗口圆角。修复：移除 header 的 `-webkit-app-region: drag`，改由 JS 监听 `mousedown/mousemove/mouseup` 并通过 `api.noteDrag` 实现拖拽；保留 `roundedCorners: true` 和 `clip-path` 保险裁剪。
  - 校验：typecheck 0 错 / test 57 全过 / build 成功；版本 V2.41.16 已 push + tag v2.41.16，GitHub Actions 自动构建。查看：Actions https://github.com/Jaycht/jingzong-react-office-0616/actions ；Release https://github.com/Jaycht/jingzong-react-office-0616/releases/tag/v2.41.16。
  - **重要经验**：用户两次反馈「V2.41.15 四项没解决」，根因是用户一直运行旧安装包（V2.41.15 或更早），V2.41.16 云端 nsis 打包在进行中（in_progress），尚未发布 Release。→ 交付 Electron 修复后，必须明确提醒用户「重新下载并覆盖安装最新版本」，否则他看到的仍是旧行为。代码层四处修复均已在 V2.41.16 验证正确（App.tsx 启动同步 closeBehavior、警徽模块 import 已生成 dist/assets/badge-icon-*.png、顶栏 grid 1fr/1fr/auto、便签 JS 拖拽+clip-path）。

## V2.41.17 修复生产包 6 项问题（2026-07-19 17:4x）
- 用户实测 V2.41.16 生产包出现 6 个问题，逐一定位根因并修复：
  1. (#2 关闭行为失效/点 X 直接退出)：根因 `electron/main.cjs` 的 `createTray()` 内 `new Tray(app.ico)` 因 app.ico 被打进 asar 内（`path.join(__dirname,"..","app.ico")` 在 asar 中指向不存在的 `resources/app.asar/app.ico`）抛错，使 `createTray` 整体中断、注册在其中的 `mainWindow.on("close")` 处理器从未注册，点 X 走 Electron 默认行为直接关窗（残留进程）。→ 关闭处理器抽成 `registerCloseHandler(win)` 在 `createWindow()` 内**无条件注册**；新增 `resolveAppIcon()` 多级兜底（extraResources 外置 `resources/app.ico` → asar 内 `createFromBuffer` → 空图），`new Tray` 用 try/catch 包裹、失败仅日志不中断；`package.json` build 增 `extraResources: ["app.ico"]` 让图标在 asar 外可用。
  2. (#5 启动变慢)：根因 #2 导致点 X 不直接退出而是关窗+残留进程，用户再启动多开实例抢占资源。→ 新增 `app.requestSingleInstanceLock()`，次实例直接 `app.quit()`，主实例 `second-instance` 聚焦已有窗口。
  3. (#3/#4 退出/恢复后数据丢失 + 恢复失败)：根因 `restoreFromJson`「先 `localStorageAdapter.clear`+`indexedDBAdapter.clear` 再写回」，一旦后续 `importAttachmentSnapshot` 抛错（生产包附件落盘失败）即返回「恢复失败: Internal error.」且数据已清空。→ 改为恢复前先对当前 localStorage+IndexedDB 做**内存快照**；写回包在 try 内、失败调用 `rollback()` 整体回滚到快照（绝不清空后失败）；附件导入失败**非致命**（仅提示「附件恢复失败，已保留其余数据」），主数据照常成功。
  4. (#6 默认头像丢失)：根因顶栏与资料弹窗默认头像用 `/avatar-default.jpg`（生产 `file://` 下 404）。→ 改 `import badgeIcon from '../assets/badge-icon.png'`（Vite 模块导入随包进 dist 带 hash），`AppLayout.tsx` 与 `ProfileModal.tsx` 默认头像均回退到警徽。
  5. (#1 随手记带提醒新记录卡片不显示)：`DailyNotes.tsx` 卡片 `motion.div` 原 `initial={{opacity:0,y:10}}` 在部分生产环境入场动画卡在不可见态。→ 改 `initial={false}`（直接渲染到 animate 终态，保证可见）；`handleSave` 包 `try/catch/finally`，无论保存是否抛错都调用 `onSaved()` 刷新列表。
- 校验：typecheck 0 错；test 57 全过；build 成功（Vite/rolldown，仅 LegalKnowledge 体积预警）；`node --check electron/main.cjs` 语法 OK。
- 升版 V2.41.17（src/version.ts + package.json），CHANGELOG 首项记录上述 6 项。
- 已 commit（分支 snapshot/v2.14.50-preplan）→ `git push origin HEAD:master`（15ffbbb..3b67247 fast-forward）→ `git tag -a v2.41.17` + `git push origin v2.41.17` 触发 GitHub Actions 云端 nsis 打包。
- 查看：Actions https://github.com/Jaycht/jingzong-react-office-0616/actions ；Release https://github.com/Jaycht/jingzong-react-office-0616/releases/tag/v2.41.17
- ⚠️ 重要交付提醒：用户须**重新下载并覆盖安装 V2.41.17**，否则运行旧包仍会复现这 6 个问题。

## V2.41.18 随手记保存健壮性补强（#1 收尾，2026-07-19 18:2x）
- 用户要求复盘检查错误 + 验证通过性（全程中文）。复盘 6 处改动全部对账正确落地：主进程 `resolveAppIcon`/`registerCloseHandler`(createWindow 无条件注册)/`createTray`(new Tray 包 try/catch)/`requestSingleInstanceLock`；`restoreFromJson` 快照+回滚+附件非致命；头像 `badgeIcon` 模块导入替换 404 路径。
- (#1 真根因深挖)：原 `handleSave` 的 `onSaved()` 并不总在 `createDailyNote()` 之后执行——适配器 `indexedDBAdapter.setItem` 虽已**同步**写内存缓存（记录确落库，故右下角提醒能正常弹），但列表 `allNotes = useMemo(getDailyNotes, [refreshKey])` 因未刷新读不到新记录，完美解释「卡片不显示但提醒正常」。V2.41.17 用 `try/finally` 让 `onSaved` 始终刷新已对症。但 `reminderData` 构造（`reminderTime.toISOString()` 等）仍在 `try` 块**之外**，构造一旦抛错整函数崩溃、`finally` 不包裹该段、`onSaved` 跳过。
- 加固：`DailyNotes.tsx` 把 `reminderData`/`data` 构造整体移入 `try` 内（双保险：构造异常也刷新列表）。
- 新增 `src/__tests__/dailyNotesStore.test.ts` 3 个回归测试：带提醒新建后 `getDailyNotes` 立即可读、update/delete 同步可见——锁定「建库即见」语义防回归。
- 校验：typecheck 0 错；test 57→**60 全过**；build 成功（仅 LegalKnowledge 体积预警非回归）；`node --check electron/main.cjs` 语法 OK。
- 升版 V2.41.18（version.ts + package.json），CHANGELOG 首项记录本次补强。
- commit（分支 snapshot/v2.14.50-preplan）→ push origin HEAD:master → tag v2.41.18 + push 触发 GitHub Actions 云端 nsis 打包。
- 查看：Actions https://github.com/Jaycht/jingzong-react-office-0616/actions ；Release https://github.com/Jaycht/jingzong-react-office-0616/releases/tag/v2.41.18
- ⚠️ 交付提醒：须**重新下载覆盖安装 V2.41.18** 验收（V2.41.17 已含 6 项主修复，V2.41.18 进一步加固 #1 收尾）。

## V2.41.19 修复 5 项用户反馈（2026-07-19 19:0x）
- 用户实测 V2.41.18 后反馈 5 项问题，逐一定位根因并修复：
  1. (#1 托盘图标不显示，无法点击回主页面)：根因 `app.ico` 源文件仅 256/128px、缺 16/32px 小尺寸，Windows 系统托盘会静默忽略大尺寸图标；`resolveAppIcon()` 在 asar 内 `createFromPath` 也读不到。→ `resolveAppIcon()` 多级兜底保留（`createFromPath` → `createFromBuffer(fs.readFileSync(asarPath))` → 空图）；`createTray()` 内 `trayIcon = base.resize({width:32,height:32})` 缩放至托盘标准 32×32，彻底解决不显示；空图 `tray=null` 兜底、异常 catch 不中断。
  2. (#2 随手记新卡片不显示)：根因 `allNotes = useMemo(getDailyNotes, [refreshKey])` 依赖 `refreshKey` 变化刷新，Electron 异步时序下 `onSaved→setRefreshKey→useMemo` 链不可靠。→ 改本地 `notes` state，便签同步/删除/批量删除/导入/保存回调共 5 处直接 `setAllNotes(getDailyNotes())` 同步重读，摆脱刷新链。
  3. (#3 便签假圆角)：用户明确"做不出真圆角就改直角"。Windows 透明窗口 `roundedCorners` + CSS `clip-path` 真圆角不可靠。→ `electron/note.html` 圆角变量 `--radius:0`、移除 `clip-path:inset(0 round var(--radius))`、折叠态 `.note.minimized` 圆角归零；`electron/main.cjs` 便签窗口 `roundedCorners:false`。便签改直角。
  4. (#4 关闭行为默认)：`App.tsx` 与 `SystemSettings.tsx` 默认 `'tray'` 改为 `'ask'`（每次询问）；主进程 `appCloseBehavior` 默认同步改 `'ask'`。
  5. (#5 全局搜索不居中)：根因顶栏 grid `minmax(0,1fr) minmax(0,1fr) auto` 左右列不对称，中间搜索列非真正窗口居中。→ 搜索框改绝对定位 `left:50%; transform:translateX(-50%)` 水平居中（最大化也居中）。
- 校验：typecheck 0 错；test **60 全过**；build 成功（仅 LegalKnowledge 体积预警非回归）；`node --check electron/main.cjs` + `preload.cjs` 语法 OK。
- 升版 V2.41.19（version.ts + package.json），CHANGELOG 首项记录上述 5 项。
- commit（单行标题 `V2.41.19 修复 托盘图标不显示/随手记卡片不显示/便签直角/关闭默认询问/搜索居中`，避免 Actions run-name 回退为 workflow 名）→ `git push origin HEAD`（当前分支 `snapshot/v2.14.50-preplan`）→ `git tag v2.41.19` + `git push origin v2.41.19` 触发 GitHub Actions 云端 nsis 打包。
- 查看：Actions https://github.com/Jaycht/jingzong-react-office-0616/actions ；Release https://github.com/Jaycht/jingzong-react-office-0616/releases/tag/v2.41.19
- ⚠️ 交付提醒：须**重新下载覆盖安装 V2.41.19** 验收（用户一直跑旧包会复现这 5 项）。

## V2.41.20 修复 #1 托盘图标 + #2 随手记卡片（真实根因，2026-07-19 19:4x）
- 用户实测 V2.41.19 后反馈：#3/#4/#5 已修复，但 #1（托盘图标仍不显示）与 #2（随手记新卡片仍不显示、需重启才显示）依旧。逐一定位**V2.41.19 未触及的真实根因**并修复：
  1. (#1 真实根因)：V2.41.19 仅做了 `resize(32×32)` + 路径兜底，但根因是 `app.ico` 同时配在 `build.extraResources:["app.ico"]` 与 `build.files:["electron/**/*", "app.ico"]`——打包后 asar 内外落点错乱，`resolveAppIcon()` 的全部路径候选（`resources/app.ico`、`__dirname/../app.ico`）与 `fs.readFileSync` 兜底**均读不到**，返回空图 → `createTray()` 走 `base.isEmpty()` 分支 `tray=null` 根本不创建托盘。→ 新增 `electron/app-icon.cjs`（将 app.ico 以 base64 内联，约 612KB），`resolveAppIcon()` **首选 `nativeImage.createFromBuffer(Buffer.from(APP_ICO_BASE64,"base64"))`** 彻底摆脱打包后路径依赖；`createTray()` 内 `resize({width:32,height:32})` 加 try/catch 兜底。`package.json` 的 `extraResources`/`files` 配置保留（不再影响）。
  2. (#2 真实根因)：V2.41.19 把列表改成本地 `allNotes` state + `setAllNotes(getDailyNotes())`，但 `src/store/dailyNotesStore.ts` 的 `getDailyNotes()` 返回的是 `indexedDBAdapter.getItem` 的**内存缓存同一引用**；`createDailyNote` 用 `notes.unshift(note)` **原地修改**该数组 → `setAllNotes(getDailyNotes())` 拿到同引用 → React `setState` 因引用相等跳过重渲染 + `useMemo([allNotes,...])` 不重算 → 卡片不刷新；重启时首渲染重算 `useMemo` 故能看到。→ `getDailyNotes()` 改为返回 `[...raw]` **副本**，确保每次新引用强制刷新卡片。
- 新增 `src/__tests__/dailyNotesStore.test.ts` #2 副本语义回归测试（2 条）：①每次调用返回新数组引用（`not.toBe`）；②原地修改返回数组不污染下一次读取（防 `unshift` 污染 UI 持有的引用）。锁定 #2 防回归。
- **诊断要点（重要）**：`indexedDBAdapter.getItem` 返回缓存同引用是「按引用读写」设计，多数 store 安全（因为 setState 传新对象），但 `dailyNotesStore` 的 `unshift` 原地修改 + 同引用返回会触发 React 跳过更新。今后类似「读取→原地改→回写→再读→setState」模式都要返回副本。
- 校验：typecheck 0 错；test **60→62 全过**（新增 2 条）；build 成功（Vite/rolldown，仅 LegalKnowledge 体积预警非回归）；`node --check electron/main.cjs`+`app-icon.cjs`+`preload.cjs` 语法 OK。
- 升版 V2.41.20（src/version.ts + package.json），CHANGELOG 首项记录 #1/#2 真实根因与修复。
- commit（单行标题 `V2.41.20 修复 托盘图标内联base64/随手记卡片返回副本/#2回归测试`）→ `git push origin HEAD`（当前分支 `snapshot/v2.14.50-preplan`）+ `git push origin HEAD:master`（fast-forward 远端 master，origin/master 当前为 9c83bae=V2.41.19）→ `git tag v2.41.20` + `git push origin v2.41.20` 触发 GitHub Actions 云端 nsis 打包。另 `git branch -f master <新提交>` 同步本地 master 引用（此前本地 master 停留在 V2.9.16，仅本地引用陈旧、不影响远端）。
- 查看：Actions https://github.com/Jaycht/jingzong-react-office-0616/actions ；Release https://github.com/Jaycht/jingzong-react-office-0616/releases/tag/v2.41.20
- ⚠️ 交付提醒：须**重新下载覆盖安装 V2.41.20** 验收（用户一直跑旧包会复现这 2 项）。

## V2.41.21 修复 受害人信息CSV导出 + 附件保存路径自定义（2026-07-19 20:3x）
- 用户两项反馈：①附件保存路径无法自定义（修改路径不可用）；②受害人信息 CSV 导出表头与数据挤在同一行。
- **#A 受害人信息 CSV（根因）**：`src/utils/excelUtils.ts` 的 `exportCsv` 用 `csvRows.join('')` 空串拼接，表头行与数据行被直接连成一行、无换行符。→ 抽纯函数 `csvToString(headers, rows, {withBom})`（便于单测），内部 `csvRows.join('\r\n')` 分行；含逗号/引号/换行的字段自动双引号包裹并转义双引号，避免破坏列结构。`exportCsv` 改为调用 `csvToString(..., {withBom:true})` 带 UTF-8 BOM（Excel 中文不乱码）。
- **#A 验证**：`scripts/verify_csv.mjs` 生成样例 `output/受害人信息_验证.csv`，确认表头一行、数据一行（如 `项目名称,受害人姓名,...` / `某集资诈骗案,李某某,男,,1234523423,,2026/06/07`），每项信息落入对应表头列。补 `src/__tests__/excelUtils.test.ts` 4 条单测锁定列结构（含逗号包裹、空值占位、BOM）。
- **#B 附件保存路径（根因）**：`electron/main.cjs` 把附件目录算成**模块级常量 `ATTACHMENTS_DIR`**（`save-attachment-file`/`safePath` 都用它），而 `set-attachments-path` IPC 只把新路径写进 `path-config.json`、**从不更新该常量**——故用户改完路径后新附件依旧写进启动时的旧目录，「修改不可用」。→ 改 `let attachmentsDir = getAttachmentsDir()`（运行时可变）；新增 `const allowedAttachmentDirs = new Set([attachmentsDir])`；`safePath` 改为遍历 `allowedAttachmentDirs`（不再单点 `startsWith`）；`set-attachments-path` 切换时 `allowedAttachmentDirs.add(attachmentsDir)` 保留旧目录可读 + `attachmentsDir = newPath` + `allowedAttachmentDirs.add(newPath)` + 写配置，旧附件仍可正常打开。
- **#B UI**：`src/components/AutoBackupPanel.tsx` 新增 `currentAttachmentsPath` state，挂载时 `getAttachmentsPath()` 真实路径显示（替换原写死的"附件存储在软件自动检测的磁盘路径下"），修改成功后 `setCurrentAttachmentsPath(result.path)` 即时刷新，副文案改为"修改后新附件即保存到所选目录（已存附件仍在原目录，可正常打开）"。preload 的 `getAttachmentsPath`/`setAttachmentsPath` 接口此前已暴露，无需改。
- **校验**：typecheck 0 错；test **62→66 全过**（新增 4 条 csvToString）；build 成功（Vite/rolldown，仅 LegalKnowledge 体积预警非回归）；`node --check electron/main.cjs`+`app-icon.cjs`+`preload.cjs` 语法 OK。
- 升版 V2.41.21（version.ts + package.json），CHANGELOG 首项记录 #A + #B。
- 发布：`git commit` 单行标题 `V2.41.21 修复 受害人信息CSV导出格式/附件保存路径自定义` → `git push origin HEAD`（snapshot/v2.14.50-preplan）+ `git push origin HEAD:master` → `git tag v2.41.21` + `git push origin v2.41.21` 触发 GitHub Actions 云端 nsis 打包。另同步本地 master 引用。
- 查看：Actions https://github.com/Jaycht/jingzong-react-office-0616/actions ；Release https://github.com/Jaycht/jingzong-react-office-0616/releases/tag/v2.41.21
- ⚠️ 交付提醒：须**重新下载覆盖安装 V2.41.21** 验收（尤其附件路径切换：新附件入新目录、旧附件仍可读）。

## Win7 衍生版评估（用户要求把 V2.41.21 同步到 E:\Deployment\MIMO\jingzong-work-log-win7）
- **结论：以主项目 V2.41.21 为基线向下兼容做 Win7 版，改动确实小很多**（用户思路正确）。根因：Win7 版已用 Electron 22 + 与主项目完全相同的 antd6/echarts6/framer-motion12/react19/zustand5/lucide-react1.16 运行时栈，且当前 V1.0.6 已经在 Win7 上跑通 → 库级兼容性已验证，主项目新业务代码（文书库/串并案/绩效/受害人CSV/附件路径）仅用标准 React/JS/IndexedDB API，无 Chromium108 不支持的语法（已 grep：无 Promise.withResolvers/Array.fromAsync/Object.groupBy；Object.fromEntries 与动态 import 在 108 可用；antd.css 实测 0 个 oklch，颜色正常）。
- **需落地的改动清单（小）**：
  1. 整体覆盖 `src/` 与 `public/`（含文书库 79docx+79pdf+79txt+22wav）为主项目 V2.41.21 版本；
  2. `electron/main.cjs`/`preload.cjs`/`notification.html`/`note.html`/`app-icon.cjs` 用主项目版，仅三处 Win7 适配：a) 不装 `electron-updater`（main.cjs 已 try/catch 优雅降级，IPC 返回"不可用"）；b) `setVisibleOnAllWorkspaces(..,{visibleOnFullScreen:true})` 加 try/catch 兜底（22 已有该参数但保险）；c) 便签窗口 `roundedCorners:false` 是 24+ 选项，22 上无害但可删；
  3. `vite.config.ts` → 必须补 `build.target: ['chrome108']`（主项目现无 target，默认偏高，否则 Win7 Chromium108 解析失败）；
  4. `package.json` → 版本升 2.41.21（建议 productName 加"-Win7版"后缀、APP_VERSION 保持 V2.41.21 与主线版本号锁步），依赖对齐主项目（echarts-for-react 主项目未实际 import，可不装；fake-indexeddb 作为 devDep 补以便测试；**不要装 electron-updater**）；electron-builder 保留 Win7 现版 24.13.3，build.files 保留 app.ico、**去掉 extraResources**（已改用 base64 内联图标，避免 asar 落点冲突）；
  5. `index.html`/`tsconfig*`/`vitest.config`/`eslint.config`/`scripts/` 同步主项目；
  6. `build/installer.nsh` 保留 Win7 现有。
- **会丢失/牺牲的效果（仅两点）**：
  1. **自动更新（检查/下载/安装）功能缺失** —— Win7 不支持可靠自更新（旧根证书 + electron-updater 在 Win7 不稳），UI「检查更新」入口需在 Win7 构建里隐藏（按 checkForUpdates 返回"不可用"判断或直接条件渲染）；
  2. **便签/提醒浮窗的半透明磨砂效果** —— Electron 透明窗口依赖 Win7 Aero(DWM)；若内网机用「基本/经典主题」(很多默认如此)，透明窗口会显示**黑色/不透明背景**而非毛玻璃（主窗口 frame:false+不透明深色底不受影响）。缓解：给 Win7 的 note.html/notification.html 加不透明背景兜底，牺牲磨砂保可读。
  - 其余（圆角窗口、Win10 风格 toast、性能）无实质损失；IndexedDB/echarts 画布/通知在 Win7 均正常。
- **待真机验证（在 Win7 机/或 Electron22 环境）**：① antd6 颜色与组件样式在 Chromium108 视觉确认；② 便签/提醒窗在 Basic 主题下的黑底问题是否出现；③ `vite build.target=chrome108` 产物可正常加载。
- 用户尚未要求立即实施，仅评估；如实施，按上述清单操作，但**Win7 版不应走 GitHub Actions tag 构建主线**（其 Actions 出的是 Win10+ 的 Electron35 包），需单独在 Win7 构建机/或支持 Electron22 的 CI 出 nsis。

