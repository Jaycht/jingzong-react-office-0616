# 2026-07-17 工作日志

## V2.22.2 ~ V2.22.3（前序，已提交）
- V2.22.2 两处界面微调：串并案分析移至时间轴下/随手记上；顶栏头像右侧显示用户名+科室。
- V2.22.3 修复会话刷新后信息丢失（App.tsx 启动恢复逻辑仅在勾选自动登录时才回填 store；改为有会话即恢复，未勾自动登录但落在 /app 时恢复数据不踢出）。新增 session.test.ts(3)，全量 41 用例。提交 89efb21。

## V2.23.0（P1-4 资金穿透/对手账户分析）
- 新增 src/utils/fundPenetration.ts：跨模块扫描全量记录，从 evidence 资金查控(freezeAmount/executeAmount)、资金分析(penetrationItems 层级/接收/转出/余额/冻结、fundSources 上游→资金账号)、mass.fundAccountDetail(收款账户)、squad.bankCardNo(银行卡号)及各类 bankAccount 字段抽取归一化银行账号，聚合为 FundAccount[](含跨案件判定 isCounterparty=疑似对手账户/跑分归集账户)，构建 FundEdge[](按穿透层级下行、上游→资金账号)。
- caseLinkage.ts 的 normBank 加 export（供 fundPenetration 复用，避免归一化分叉）。
- 新增 src/pages/FundPenetration.tsx：KPI 概览 + 资金流向有向图(echarts graph，账户节点+层级流向边+金额标签，复用 CaseGraph 渲染模式) + 账户雷达表格(脱敏/案件/记录/接收/转出/余额/冻结/判定，疑似对手账户置顶红标) + 筛选(仅看对手账户/模块/搜索) + 点击账户弹关联记录抽屉并复用 CaseDetail。
- 导航：departments.ts PLATFORM_NAV.top 在 linkage 下新增 fund(Landmark 图标)；AppLayout.tsx lazy+PAGES；Breadcrumb 经 PLATFORM_NAV 自动解析。
- Dashboard 新增「资金穿透线索」面板(疑似对手账户 top5，点击跳 fund 页)。
- 版本 V2.23.0（version.ts + package.json 同步，CHANGELOG 首条）。新增 fundPenetration.test.ts(5 用例)。
- 验证：typecheck 0 错误 / build 成功(FundPenetration 分包 26.74kB) / test 46/46 全过。提交 1f93ce8（9 文件 +820/−6）。CloudStudio 同链接部署成功。

### 下一步（待用户确认）
P1-5 趋势与绩效分析（按月案件量趋势、办结率趋势、各经办人工作量/超期率）。长线 P1-6/P1-7 与 P2 体验项（见 MEMORY.md 待做范围）。

## V2.23.1（取消 P1-4 资金穿透/对手账户分析）
- 用户评估后认为真正的资金穿透需导入外部银行流水反馈压缩包(多银行 CSV、账户信息表)并做表格结构识别，复杂度过高、投入产出比低，主动要求取消。
- 整体回滚 V2.23.0 新增项：删除 src/pages/FundPenetration.tsx、src/utils/fundPenetration.ts、src/__tests__/fundPenetration.test.ts(5用例)；departments.ts 移除 fund 导航项；AppLayout.tsx 移除 lazy 导入/PAGES 的 fund/PLACEHOLDER_PAGES 的 fund；Dashboard.tsx 移除资金穿透面板 JSX + buildFundModel import + counterparties useMemo + Landmark 图标导入。
- 侧栏导航恢复为 工作台→案件图谱→案件时间轴→串并案分析→日常随手记。
- 版本 V2.23.1（version.ts + package.json 同步，CHANGELOG 首条说明取消原因）。验证：typecheck 0 错误 / build 成功 / test 41/41 全过（46→41，回退 5 资金用例）。提交 a0f6d38（8 文件 +7/−817）。CloudStudio 同链接部署成功。
- MEMORY.md 待做范围已标记 P1-4 ❌已取消，并记录"若重提须以独立银行流水导入子项目形式"；与 UserManage/官式文书一并列入"不要再提出"清单。

## V2.24.0（P1-5 趋势与绩效分析）
- 新增纯函数 src/utils/performanceStats.ts：isRecordCompleted(归一化 status/handleStatus 含办结/完成/已结)、getRecordHandler(归一化 handler/leadOfficer/handlingOfficer/handlerPolice)、buildMonthlyTrend(近12月案件量+办结率)、buildHandlerPerf(经办人聚合+超期率，基于预警 overdue/critical 集合)。
- echarts.ts 注册 LineChart（趋势折线）；Dashboard 新增「办案趋势(近12月)」双轴图(案件量柱+办结率折线) 与「经办人绩效」面板(负责/超期/超期率，红橙绿着色)；PanelHead 增加 sub 副标题支持。
- 修复案件类型饼图长期缺陷：此前只统计 4 个案件类模块(mass-statistics/legal-report-case/legal-case-ledger/squad-case)，现改为统计全部含 caseType 字段的记录。
- 版本 V2.24.0（version.ts + package.json 同步）。新增 performanceStats.test.ts(16用例)。验证：typecheck 0 错误 / build 成功(LineChart 注册致 echarts 分包 578→599kB) / test 57/57 全过。提交 76f7a31（6 文件 +330/−12）。CloudStudio 同链接部署成功。
- MEMORY.md 待做范围已标记 P1-5 ✅ 与 P1-6 ✅(useReminderService 已实质落地，无需再补)；下一优先 = P1-7 录入校验 或 P2「我的视图/筛选保存」。

## V2.25.0（备份/恢复向导强化，用户从参谋新增建议清单中选定）
- 现状复查：项目早已有 `src/pages/Backup.tsx`（备份与恢复页）+ `src/components/AutoBackupPanel.tsx`（自动备份/路径配置），`excelUtils.ts` 的 `generateBackup/restoreFromJson` 已全量备份(localStorage `jingzong.*` + IndexedDB `jingzong.*` + 附件二进制)并恢复；经核对 `indexedDBAdapter` 全部实际 key（massStore/dailyNotesStore/draftStore）恰好是 dailyNotes/mass.records/draft.*，restoreFromJson 的写回逻辑已覆盖，**无丢数据 bug**（初判误报）。
- 强化点（用户选"备份/恢复向导"方向）：①`generateBackup` 在 Electron 下改调 `window.electronAPI.showSaveDialog` 弹原生保存框，可自选任意路径(如U盘/备份文件夹)，浏览器回退 `saveAs`；返回 boolean 区分取消/成功；备份文件写入 `appVersion` 与 `idbKeys` 清单。②`restoreFromJson` 改用备份自带 `idbKeys` 决定写回 IndexedDB 的键（兼容旧备份回退 dailyNotes/mass.records + draft.*），消除未来新增存储键漏恢复风险。③新增 `previewBackupFile` 解析备份头(时间/版本/记录数/附件数/大小)。④`Backup.tsx` 改向导式：备点取消给 info toast；恢复改为 先 previewBackupFile → Modal.confirm 显示来源信息与不可撤销警告 → 确认才 restoreFromJson → 成功后 `location.reload()` 使会话/store 从新数据重初始化。
- 版本 V2.25.0（version.ts + package.json 同步，CHANGELOG 首条）。验证：typecheck 0 错误 / build 成功 / test 57/57 全过（无回归）。提交 9710746（4 文件 +147/−28）。CloudStudio 同链接部署成功。
- 注：Electron 原生路径选择须桌面版验证（Web 预览走 saveAs 回退）；MEMORY.md 待做范围已补"备份/恢复向导 ✅"，下一优先仍为 P1-7 录入校验 或 P2 我的视图/筛选保存。

## V2.26.0（离线文书库，用户从参谋新增建议清单中选定"提供离线版文书下载"）
- 素材来源：用户上传 ①`D:\下载\4202661` 目录 54 份 PDF = 公安行政法律文书式样(2012版) 式样一~四十六 + 若干清单（其中 [54] 是"公安部办公厅2012年12月28日印发"落款页，非文书，排除）；②10 份最新修正版刑事法律文书 PDF（诉讼权利义务告知书×3、提请批准逮捕书、起诉意见书、扣押清单、查封/解除查封清单、准予/不准予补充鉴定重新鉴定决定书、准许被取保候审人离开所居市县决定书）。两类按名称无重复（行政无扣押/查封清单，刑事无），最终 行政 50 + 刑事 10 = 60 份互补文书。
- 关键排错：初判 temp 目录 PDF 为图片型（pdftotext 默认抽 0 中文），后经 `pdffonts/pdftotext -enc UTF-8` 字节层确认实为文字型（12 字体对象 + 4 ToUnicode，0 图片 XObject），中文完好可读；"0 中文"系 pdftotext 默认编码(latin1)处理 CJK 错乱所致。本机无 LibreOffice/ghostscript，但 `pdftotext -enc UTF-8` 可用，pypdf 经隔离 venv(pip) 装上用于合并。
- 资产整理（/tmp/ingest_forms.py + pypdf）：①用 `pdftotext -enc UTF-8` 抽取每份首页识别 式样号+文书名；②3 组续页用 pypdf 合并为完整单文件（15+16 行政处罚告知笔录、21+22 治安调解协议书、25+26 行政处罚决定书，各 2 页）；③按内容重命名、分类复制到 `public/forms/行政`(50) 与 `public/forms/刑事`(10)，排除 [54]；④生成 `public/forms/manifest.json`（title/category/shiyang/file）。全部 60 份 pypdf 可解析通过。
- 新增「文书库」模块：src/pages/LegalForms.tsx（分类 全部/行政/刑事 + 搜索框按文书名/式样号 + KPI 概览 + 卡片网格，预览=window.open 同源 PDF、下载=<a download>）；manifest.json 经 fetch 驱动。PLATFORM_NAV.top 加 `legalForms`(Gavel 图标) 一级入口（注意：原 PLATFORM_NAV.data 分组在 Sidebar 未渲染，backup/attachments 等实际挂在 SystemSettings 标签页，故文书库走 top 直接入口更醒目）；AppLayout 加 lazy+PAGES。延续大厂仪表盘风（wb-kpi/wb-panel/dash-action）。
- 版本 V2.26.0（version.ts + package.json 同步）。验证：typecheck 0 错误 / build 成功(LegalForms 分包 14.65kB) / test 57/57 全过。提交 d03e0bd。dist/forms 含 60 PDF+manifest；.gitignore 不排 public，随包提交。CloudStudio 同链接部署成功。
- 范围澄清（写入 MEMORY.md）：此为**离线空白模板参考/下载库**，非"自动填充生成制式文书"（后者内网有工具、仍不做）；故不与原 P0-2 砍掉决策冲突。MEMORY.md 待做范围补"文书库 ✅"，测试数 27→57 修正，下一优先仍为 P1-7 录入校验 或 P2 我的视图/筛选保存。
- 待桌面版验证点：文书库预览/下载在 Electron 走同源 URL（window.open/`<a download>`），桌面端建议补 `electronAPI.openPath` 原生打开 handler（当前主进程未暴露，可作后续增强）。

## V2.27.0（文书库升级：分类修正 + 双格式 + 刑事补充）
- 用户指令：把 `D:\下载\制式文书.doc`（纯行政 54 页 11417 词）逐份抽离加入文书库并提供 PDF+Word 双格式；并修正此前"行政/刑事"二元分类错误（受案登记表/受案回执/接受证据清单/传唤证/询问讯问笔录等行政刑事通用），要求上网核实官方分类边界并补充缺失文书。
- 分类模型升级：manifest.json 的 `category` 由字符串改为**字符串数组**，新增「通用」类（行政刑事通用，带*号，共 15 种）；行政/刑事筛选时通用文书同时出现。`LegalForms.tsx` 已重写：`category: string[]`、四分类 Segmented(全部/行政/刑事/通用)、`matchCat` 通用逻辑、KPI 增「行政刑事通用」、卡片多分类标签(通用蓝/行政绿/刑事紫)、底部预览/PDF/Word 三按钮。
- 双格式落地：①行政 50 份 Word 由 LibreOffice 把用户 `制式文书.doc`→`制式文书.docx` 后按标题块忠实拆分为独立 docx（保留表格版式，/tmp/split_docx.py）；②5 份因在源 doc 中与邻页合并漏拆（受案回执/移送案件通知书/没收违法所得非法财物清单/解除社区戒毒社区康复通知书/强制执行申请书），改用 pypdf 抽官方 PDF 文本 + python-docx 忠实重建（含清单表格，/tmp/make_admin_docx.py）；③刑事 10 份「最新修正版」官方 PDF 原为 PDF 文本重建 Word，保留。全部 79 份均有 PDF+Word。
- 刑事文书补充（用户授权"从网上搜搜补充进来"）：经 WebSearch 核实官方《公安机关刑事法律文书式样(2020版)全100种》完整目录与公法制〔2020〕1009号。本环境无法可靠下载全部官方空白 PDF（公安部官网仅附 10 个修订/新增文书，整本无单一可信下载源），故基于已核实的 2020 版官方式样结构，用 python-docx 重建 19 份核心刑事空白参考模板（/tmp/make_criminal_docx.py）并经 LibreOffice docx→pdf 批量转 PDF：立案决定书/不予立案通知书/拘留证/拘留通知书/延长拘留期限通知书/逮捕证/逮捕通知书/取保候审决定书·执行通知书/监视居住决定书·执行通知书/搜查证/通缉令/释放通知书/释放证明书/扣押决定书/查封决定书/鉴定意见通知书/撤销案件决定书/终止侦查决定书/移送起诉告知书。
- 透明说明：这 19 份为「按公安部 2020 版式样重建的参考模板」，**非官方原版扫描件**；若用户后续提供整本《刑事法律文书式样》来源文件（如做行政 doc 那样），可像拆分行政 doc 一样精确拆分替换。其余约 60 种刑事专属文书（律师参与/技术侦查/执行/刑事通用等）暂未覆盖。
- manifest 校验：79 条目，行政 50 / 刑事 29 / 通用 15（条目级计数，含「行政+通用」「通用」重叠），0 文件缺失。shiyang 字段对生成的 19 份留空。
- 版本 V2.27.0（version.ts + package.json 同步，CHANGELOG 首条）。验证：typecheck 0 错误 / test 57/57 全过 / build 成功(LegalForms 分包 15.71kB)。dist/forms 含 79 双格式 + manifest。
- 工具链固化：LibreOffice 26.2.4 在 `C:\Program Files\LibreOffice\program\soffice.exe`，docx→pdf 可靠（writer_pdf_Export），但 **PDF→docx 在本机构导出过滤器不可用**；pdf2docx pip 安装过慢(>4min)未采用；故 PDF 源→Word 统一走 pypdf 抽文本 + python-docx 重建（隔离 venv `C:\Users\Infer\.workbuddy\binaries\python\envs\default`，pypdf 6.14.2 / python-docx 1.2.0）。
