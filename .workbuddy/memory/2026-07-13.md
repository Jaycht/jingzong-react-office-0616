# 2026-07-13 工作日志

## 五大业务部门全面体检 → 按方案修复优化（V2.14.38）
执行体检报告提出的全部修复方案（用户指令"按照你的方案修复优化"），覆盖大队办公室/涉众办/法制室/案件中队/调证分析。

### 已落地修复（Tasks #74–#78）
1. **配置层真实 bug（字段缺失回落通用模板）**
   - `fields/office.ts`：新增 `office-party-attendance`（三会一课/主题党日/组织生活/党员管理/考勤登记/值班加班 6 类）与 `office-doc-report`（涉密文件/非涉密文件/报表上报/材料归档/公文处理/通知传达/会议精神/工作反馈/落实跟踪 9 类，含 handleStatus select）分支，消除原回落通用模板丢字段。
   - `fields/legal.ts`：新增 `legal-assessment`（考核标准/考核对象/结果登记/整改建议 4 类）分支，此前该模块无定义→回落通用模板。
   - `departments.ts`：import `Footprints`；涉众办末位注册 `mass-visit`（来访登记，群众来访/诉求登记/处置跟踪），启用此前 mass.ts 有定义但未注册的死代码模块。
2. **卡片状态徽标假数据**（`ModulePage.tsx`）：原恒为"办理中"，改从真实状态字段（select 且 label 含 状态/进度/结案/归档/报销/反馈/整改/结果）派生；统计卡片"已办结/办理中"按真实已办结数计算；无状态字段则不显示徽标且隐藏状态筛选。
3. **暗色硬编码发白**（`ModulePage.tsx` + `DrawerNewRecord.tsx`）：标题/副标题/图标/边框/可重复 section 卡片/附件下载链接/步骤指示器/模块选择框等约 20 处硬编码色替换为 CSS 变量（--color-text / --color-text-secondary / --color-border-light / --color-surface-hover / --color-primary 等），深色模式正确跟随主题。
4. **附件孤儿清理**：`attachmentStore` 新增 `deleteAttachmentsByRecord(recordId)`；`massStore` 的 `deleteMassRecord(s)` 联动调用清理，`massStore` 新增 `cleanupOrphanAttachments()`（清理 recordId==='pending' 或指向不存在记录的附件）；`SettingsPage` 新增「附件维护」面板 + 「清理孤儿附件」按钮（loading 态 + toast）。
5. **时限提醒覆盖缺口**（`constants/legalDeadlines.ts`）：新增 `petition-legal-deadline`（信访法定办结 60 日，moduleIds=['mass-petition']，dateField='legalDeadline'）与 `freeze-expire`（冻结到期，moduleIds=['evidence-freeze']，dateField='expireDate'），扩展 useReminderService 预警至涉众办信访与调证分析冻结。

### 版本与验证（Task #79）
- `src/version.ts`：V2.14.37 → V2.14.38，CHANGELOG 首条插入上述 5 条修复说明；`package.json` version 同步 2.14.38。
- 验证：`npx tsc -p tsconfig.app.json --noEmit --skipLibCheck` 0 错误（修复 legal.ts 一处 `_tab`/`tab` 笔误）；`npm run test` 27/27 通过；`npm run build` 成功。
- 部署：CloudStudio 重新部署同一链接 https://d9f5609727dc445c8032be76d7f7f975.app.codebuddy.work

### 注意
- 新增模块字段分支时务必使用函数参数名（legal.ts 为 `_tab`，office.ts/mass.ts 为 `tab`）—— 曾因笔误 `tab` 触发 tsc 未定义变量错误。
- 复利提醒：每次改 moduleConfig 的 fieldsFor 分支后必须跑 tsc（rolldown 对未导入自由变量静默放过，曾致白屏）。
